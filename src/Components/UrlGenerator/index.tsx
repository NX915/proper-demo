import React from 'react';

export default function UrlGenerator() {
  /*
  Generate short url for a saved list to allow access or sharing with or without an account
  May need to support different url generation api in backend for lists that only saved property IDs
  and advanced lists with saved historial information such as list price at the time of saving
  */
  return <div></div>;
}
